import { EnumToSelectEntriesPipe } from './enum-to-select-entries.pipe';

describe('EnumToSelectEntriesPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumToSelectEntriesPipe();
    expect(pipe).toBeTruthy();
  });
});
