import { GetAsFormArrayPipe } from './as-form-array.pipe';

describe('AsFormArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new GetAsFormArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
