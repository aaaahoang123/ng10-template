import { ConnectedFormDirective } from './connected-form.directive';

describe('ConnectedFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectedFormDirective();
    expect(directive).toBeTruthy();
  });
});
